---
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install dependencies and build
        run: |
          npm install
          npm run build
  test:
   runs-on: ubuntu-latest
   needs: build
---
on:
  pull_request:
    branches: [ recette ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install dependencies and build
        run: |
          npm install
          npm run build
  test:
   runs-on: ubuntu-latest
   needs: build
   steps:
        uses: actions/checkout@v3
      - name: Test
        run: |
          npm install
          npm install request
          npm run test
          docker login ghcr.io -u sophieaccount -p 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
          docker build -t ghcr.io/sophieaccount/nodejs-my-website:latest ./
'on': [ push ]